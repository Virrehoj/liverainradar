# Replace with your real domain. Caddy provisions and renews the Let's
# Encrypt certificate automatically the first time it starts.
nederbord.example.com {
    reverse_proxy 127.0.0.1:8000
    encode gzip
}
